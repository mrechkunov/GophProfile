Критерии приёмки
Перед отправкой кода на ревью проверьте себя по этому чек-листу. Если хотя бы одно условие чек-листа не выполнено, ревьюер вернёт проект на доработку.
Деплой:
+ Все компоненты успешно деплоятся в Kubernetes
+ Health checks работают корректно
+ Сервис доступен через Ingress
Масштабируемость:
+ HPA корректно масштабирует поды hpa.yaml
+ Load balancing работает между репликами
+ Graceful shutdown реализован через signal.NotifyContext в воркере и в app service
Мониторинг:
+ Метрики собираются в Kubernetes окружении
+ Дашборды отображают состояние кластера в grafana
+ ServiceMonitor корректно настроен альтернатива - настроен otel-collector который собираем метрики и трейсы с приложения
Безопасность:
+ NetworkPolicy ограничивает трафик networkpolicy.yaml ограничивает доступ к приложению и бд
+ Секреты используются для конфиденциальных данных файл values-secret.yaml передается при установке helm чарта, добавлен в .gitignore но для PR строка закоменчена
+ Используются non-root контейнеры

Общие критерии к сервису

Документация:
+ README с инструкциями по запуску
+ API документация (OpenAPI/Swagger) openapi.yaml
+ Архитектурная диаграмма shema.png
+ Описание мониторинга и алертов по коду приложения

Production-готовность:
+ Graceful shutdown
+ Circuit breaker для внешних зависимостей
+ Rate limiting
+ Proper error handling
+ Resource limits настроены